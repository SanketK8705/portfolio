name: Sync LeetCode Stats

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fetch LeetCode stats
        run: |
          curl -fsSL "https://alfa-leetcode-api.onrender.com/sanket_kumar_singh/solved" -o /tmp/solved.json
          curl -fsSL "https://alfa-leetcode-api.onrender.com/sanket_kumar_singh/calendar" -o /tmp/calendar.json
          jq -s '.[0] * .[1]' /tmp/solved.json /tmp/calendar.json > /tmp/leetcode-stats.json
          jq empty /tmp/leetcode-stats.json
          mkdir -p assets
          mv /tmp/leetcode-stats.json assets/leetcode-stats.json

      - name: Commit updated stats
        run: |
          if git diff --quiet -- assets/leetcode-stats.json; then
            echo "LeetCode stats unchanged."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/leetcode-stats.json
          git commit -m "Sync LeetCode stats"
          git push
